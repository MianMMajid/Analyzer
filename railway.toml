# Shared Railway defaults. Service-specific build/start commands are set as
# Railway variables so backend, frontend, and refresh can deploy from one
# monorepo root without breaking workspace package imports.

[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
